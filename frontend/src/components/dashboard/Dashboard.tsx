import React from "react";
import { Card } from '@material-ui/core';
import { Title } from 'react-admin'

export default () => {
     return (
          <Card>
               <Title title="" />
          </Card>
     )
}