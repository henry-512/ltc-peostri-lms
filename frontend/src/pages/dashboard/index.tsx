const Dashboard = () => {
    let permission = "";

    switch (permission) {
        case 'something':
            return (<></>)
        default:
            return <Dashboard />
    }
}

export default Dashboard;